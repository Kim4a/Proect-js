// НАХОЖДЕНИЕ ДИСКРИМИНАНТА

// const a = prompt("Введите a");
// const b  = prompt("Введите b");
// const c  = prompt("Введите c");
// let D = 0;

// D = ((Number(b)*Number(b))-4*Number(a)*Number(c));

// alert (D);

// if (D = Number)
// {
// alert (D);
// } else {
//     alert("Ошибка");
// }

// ЗАДАНИЕ СО СТРОКОЙ

let n = 5;
const someStr = [3, 1, 5, 8, 2];
let max = 0;
let min = 0;

for (let i = 0; i < n; i++) {
    if (max > someStr[i]) {
        max = someStr[i] 
    }
}

alert(max);
